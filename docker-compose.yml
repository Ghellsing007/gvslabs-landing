version: '3.8'
services:
  gvslabs-landing:
    build: .
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost/
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    expose:
    - '80'
